const prefix = 'http://localhost:3000';
export const AUTH_SERVER = `${prefix}/api/auth`;
export const CONTENT_SERVER = `${prefix}/api/content`;
export const EVENTS_SERVER = `${prefix}/api/events`;
export const TERMS_SERVER = `${prefix}/api/terms`;